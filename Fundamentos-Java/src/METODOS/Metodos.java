package METODOS;

public class Metodos {

    //ejemplo.equalsIgnoreCase -> Para que ignore si es mayuscula o minuscula
    //Math.pow(base,exponente) -> Para que saque la base por el exponente de un numero
    //Collection.min(ListaEjemplo) ->
    //Collection.max(ListaEjemplo) ->
    //ejemplo.length() ->
    //.trim() -> Quitar los espacios al inicio y al final
    //ejemplo.toUpperCase() -> Para pasarlo todo a  mayusculas.
    //ejemplo.isEmpty() -> Para saber si esta vacio
    //ListaEjemplo.add(ejemplo) -> Para añadir a una lista
    //ListaEjemplo.contains(ejemplo) -> Para saber si esta en la lista
    //ListaEjempllo.size() ->
    //Collections.sort(ListaEjemplo) -> para ordenar una lista alfabeticamente
    //ejemplo.charAt() ->
    //Math.exp(ejemeplo) -> Para sacar el numero elevado a e
    //Math.sqrt(ejemplo) -> Para sacar la raiz cuadrada de un numero
    //Math.floor(ejemplo) -> Para redondear un numero con decimales
    //ejemplo.indexOf(ejemplo2) ->
    //ejemplo.replace("a","A") -> Para cambiar una palabra, letra o numero por otra.
    //ejemplo.concat() -> Para unir las palabras
    //ejemplo.endWith("ar") -> Para saber si una palabra termina con lo introducido
    //ejemplo.startWith(ejemplo2) ->Para saber si una palabra empieza por lo introducido
    //

}
